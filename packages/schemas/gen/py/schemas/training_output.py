# generated by datamodel-codegen:
#   filename:  training_output.json

from __future__ import annotations

from pydantic import BaseModel, Field


class TrainingOutput(BaseModel):
    output_path: str = Field(
        ..., description='The S3 path where the training job saves the model artifacts.'
    )
    completed_training_units: int = Field(
        ..., description='The number of training units completed.'
    )
    is_completed: bool = Field(
        ...,
        description='Indicates whether the training job has completed, when all training units are  carried out',
    )
